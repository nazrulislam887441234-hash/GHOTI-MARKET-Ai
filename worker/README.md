# GHOTI MARKET AI Cloudflare Worker & Frontend

This project provides a production-ready AI Assistant backend built with Cloudflare Workers, `@google/genai` SDK, and Gemini Interactions API (`antigravity-preview-09-2026`), paired with a responsive, branded frontend (`#FF6B35`).

## Setup & Deployment Instructions

### 1. Prerequisites
- Node.js installed locally.
- Cloudflare account with Wrangler CLI logged in (`npx wrangler login`).

### 2. Backend Setup (`worker/`)
1. Navigate to the worker directory:
   ```bash
   cd worker
